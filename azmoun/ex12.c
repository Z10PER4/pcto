#include <stdio.h>
int main()
{
    int a = 1;
    while (a<=100)
    {
        printf("%d\n", a);// può essere scritto pure a+1, ma in tal caso il while è solo (a<100)//
        a=a+1;//a++//a+=1//
    }
    
}